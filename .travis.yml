os: osx
sudo: required
language: minimal

addons:
  homebrew:
    packages:
      - make
    casks:
      - mactex-no-gui
    update: true

jobs:
  include:
    - stage: compile
      name: "Compile documents"
      script:
        - make
