sudo: required
language: generic

services:
  - docker

stages:
  - compile

jobs:
  include:
    - stage: compile
      name: "Compile documents"
      install:
        - pip install awscli --upgrade
      script:
        - make build
        - make update
      if: tag IS present
